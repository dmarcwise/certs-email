# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Email templates: plain-text formatting is intentional, and Prettier's
# Handlebars parser mangles the HTML ones (drops <!DOCTYPE>, rewrites quotes).
src/templates/*.hbs
